# Changelog

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)，而版本號遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

## [Unreleased]

### Feature

- 按全域快捷鍵（預設 `Alt+.`）叫出搜尋視窗，視窗會出現在目前使用中的螢幕，按 Enter 把內容送進原本作用中的程式
- 片語、書籤、密碼存在同一個 KeePass kdbx 檔，也能用 KeePassXC 開啟
- 片語支援動態佔位符：日期時間（可偏移、可自訂格式）、剪貼簿、送出後的游標位置、輸入欄位、下拉選單、UUID、引用其他片語
- 開啟保險庫的金鑰快取在 Windows 認證管理員，開 app 不必輸入主密碼；密碼欄位另外用公鑰加密，存密碼不必輸入主密碼，讀取或送出時才需要
- 密碼與帳號逐字輸入、不經過剪貼簿；片語與書籤貼上時，會排除 Windows 剪貼簿歷史與雲端剪貼簿
- 片語與書籤貼上後，還原原本的剪貼簿文字
- 密碼項目按 Enter 可選擇送出帳號或密碼；預覽的帳號、網域、密碼旁各有複製鈕
- 依類別（片語、書籤、密碼）過濾搜尋結果，按 Tab 切換
- 在搜尋框打 # 會跳出標籤選單，選好的標籤顯示成搜尋框裡的小方塊；按住 Tab 再按 ↑ ↓ 切換工作區
- 設定頁新增快捷鍵設定，按下組合鍵就能更改所有快捷鍵，全域快捷鍵改完立刻生效
- 項目可以分工作區，用搜尋框左側的下拉選單切換，在設定頁新增、改名、刪除
- 按 Ctrl+Enter 用預設瀏覽器開啟書籤，只開 http、https 網址
- 從 Chrome、Edge、Firefox 匯入書籤，會逐一列出每個瀏覽器設定檔
- 匯入其他密碼管理器匯出的 CSV（Chrome、Firefox、Bitwarden、1Password、KeePassXC、LastPass 等），匯入完成後可以直接在設定頁刪除 CSV
- 內建密碼產生器，可設定長度與字元類別、排除易混淆字元、要求每類至少一個
- 閒置一段時間後自動鎖定密碼，片語與書籤照常可用；可以更換主密碼
- 存檔時先寫暫存檔並讀回逐欄比對，比對一致才覆蓋正本，並保留 3 份備份
- 偵測 OneDrive、iCloud 產生的同步衝突副本並提示
- 第一次啟動有設定精靈，選擇保險庫位置並設定主密碼
- 介面支援繁體中文與 English，預設跟隨系統語系；主題可選淺色、深色或跟隨系統
- 關掉視窗後程式留在系統匣；保險庫已開啟時再點一次執行檔，只會跳通知，不開第二個

[Unreleased]: https://github.com/John8805/InkKeep/commits/main
